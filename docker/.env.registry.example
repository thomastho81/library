# Opcional. O compose de registry já usa thomastho81/library:main por defeito.
# Para fork ou tag diferente, copie para docker/.env (nome reconhecido pelo Compose):
#
#   cp .env.registry.example .env
#
# e edite os valores.

GHCR_OWNER=thomastho81
GHCR_REPO=library
IMAGE_TAG=main
